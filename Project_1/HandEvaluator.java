
public class HandEvaluator {
	
	public Card[] generateDeck(int totalCards) {
		Card[] deck = new Card[totalCards];
		int maxSuitType = 13;
		int maxValueType = 1;
		for(int i = 0; i < maxSuitType; i++) {
			//Assign all values for HEARTS
			
		}
		
		
		return deck;
	}
}
